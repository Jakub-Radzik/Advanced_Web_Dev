export type ClientForm = {
    email: string,
    termsOfService: boolean,
    phone: string,
    firstName: string,
    lastName: string,
};

export type LoginForm = {
    username: string,
    password: string
}