import { Navbar as MantineNavbar } from "@mantine/core";

export const Navbar = () => {
  return (
    <MantineNavbar width={{ base: 300 }} height={500} p='xs'>
      {/* Navbar content */}
    </MantineNavbar>
  );
};
